@* @model IEnumerable<Pyme.Abstracciones.ModelosParaUI.ProductoDto> *@

@* @{
    ViewBag.Title = "Listado de Productos";
}

<div class="w-full px-6 py-8">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
        <h1 class="text-2xl font-bold text-[color:var(--txt-primary)]">@ViewBag.Title</h1>

        <div class="flex items-center gap-3 mt-4 sm:mt-0">
            <a href="@Url.Action("CrearProducto")"
               class="px-4 py-2 rounded-xl text-white font-medium bg-[color:var(--primary)] hover:bg-[color:var(--primary-dark)] transition-colors">
                <i data-lucide="plus" class="inline w-4 h-4 mr-1"></i> Nuevo Producto
            </a>

            <a id="alertaProductos"
               href="#"
               class="px-4 py-2 rounded-xl border border-[color:var(--primary)] text-[color:var(--primary)] hover:bg-[color:var(--primary)] hover:text-white transition-colors font-medium">
                <i data-lucide="alert-circle" class="inline w-4 h-4 mr-1"></i> Alerta
            </a>
        </div>
    </div>

    <div class="w-full overflow-x-auto shadow-sm rounded-2xl border border-[color:var(--border)] bg-[color:var(--light-panel)]">
        <table class="min-w-full text-sm text-left text-[color:var(--txt-secondary)]">
            <thead class="bg-[color:var(--light-bg)] text-[color:var(--txt-primary)] text-xs uppercase">
                <tr>
                    <th class="px-6 py-3 text-center font-semibold">Acciones</th>
                    <th class="px-6 py-3 font-semibold">Nombre</th>
                    <th class="px-6 py-3 font-semibold">Categoría</th>
                    <th class="px-6 py-3 font-semibold text-right">Precio</th>
                    <th class="px-6 py-3 font-semibold text-right">Impuesto (%)</th>
                    <th class="px-6 py-3 font-semibold text-right">Stock</th>
                    <th class="px-6 py-3 font-semibold">Imagen</th>
                    <th class="px-6 py-3 font-semibold">Estado</th>
                </tr>
            </thead>

            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var producto in Model)
                    {
                        <tr class="border-t border-[color:var(--border)] hover:bg-[color:var(--light-bg)] transition-colors">
                            <td class="px-6 py-4 text-center">
                                <div class="flex items-center justify-center gap-4">
                                    <a href="@Url.Action("EditarProducto", new { id = producto.Id })"
                                       class="text-[color:var(--primary)] hover:text-[color:var(--primary-dark)] transition-colors"
                                       title="Editar">
                                        <i data-lucide="pencil-line" class="w-5 h-5"></i>
                                    </a>
                                    <a href="@Url.Action("EliminarProducto", new { id = producto.Id })"
                                       class="text-[color:var(--error)] hover:text-red-700 transition-colors"
                                       title="Eliminar">
                                        <i data-lucide="trash-2" class="w-5 h-5"></i>
                                    </a>
                                    <a href="@Url.Action("DetallesProducto", new { id = producto.Id })"
                                       class="text-[color:var(--secondary)] hover:text-emerald-600 transition-colors"
                                       title="Ver Detalles">
                                        <i data-lucide="eye" class="w-5 h-5"></i>
                                    </a>
                                </div>
                            </td>

                            <td class="px-6 py-4">@producto.Nombre</td>
                            <td class="px-6 py-4">@producto.CategoriaNombre</td>
                            <td class="px-6 py-4 text-right">₡@producto.Precio.ToString("N2")</td>
                            <td class="px-6 py-4 text-right">@producto.ImpuestoPorc.ToString("N2")%</td>
                            <td class="px-6 py-4 text-right">@producto.Stock</td>
                            <td class="px-6 py-4">
                                @if (!string.IsNullOrEmpty(producto.ImagenUrl))
                                {
                                    <img src="@producto.ImagenUrl" alt="@producto.Nombre" class="w-12 h-12 rounded-lg object-cover" />
                                }
                                else
                                {
                                    <span class="text-gray-400 italic">Sin imagen</span>
                                }
                    </td>
                    <td class="px-6 py-4">
                        <span class="px-3 py-1 rounded-full text-xs font-medium
                                    @(producto.EstadoProducto == "Activo"
                                                                      ? "bg-emerald-100 text-emerald-700"
                                                                      : "bg-red-100 text-red-700")">
                            @producto.EstadoProducto
                        </span>
                    </td>
                </tr>
                                }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="px-6 py-6 text-center text-gray-400">
                            No hay productos registrados.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
 *@

@{
    ViewBag.Title = "Tabla de Prueba";
}

<div class="w-full px-6 py-8">
    <h1 class="text-2xl font-semibold text-[color:var(--txt-primary)] mb-6">@ViewBag.Title</h1>

    <div class="w-full overflow-x-auto shadow-sm rounded-2xl border border-[color:var(--border)] bg-[color:var(--light-panel)]">
        <table class="min-w-full text-sm text-left text-[color:var(--txt-secondary)]">
            <thead class="bg-[color:var(--light-bg)] text-[color:var(--txt-primary)] text-xs uppercase">
                <tr>
                    <th class="px-6 py-3 text-center font-semibold">Acciones</th>
                    <th class="px-6 py-3 font-semibold">Nombre</th>
                    <th class="px-6 py-3 font-semibold">Correo</th>
                </tr>
            </thead>

            <tbody>
                <tr class="border-t border-[color:var(--border)] hover:bg-[color:var(--light-bg)] transition-colors">
                    <td class="px-6 py-4 text-center">
                        <div class="flex items-center justify-center gap-4">
                            <a href="#" class="text-[color:var(--primary)] hover:text-[color:var(--primary-dark)] transition-colors" title="Editar">
                                <i data-lucide="pencil-line" class="w-5 h-5"></i>
                            </a>
                            <a href="#" class="text-[color:var(--error)] hover:text-red-700 transition-colors" title="Eliminar">
                                <i data-lucide="trash-2" class="w-5 h-5"></i>
                            </a>
                            <a href="#" class="text-[color:var(--secondary)] hover:text-emerald-600 transition-colors" title="Ver Detalles">
                                <i data-lucide="eye" class="w-5 h-5"></i>
                            </a>
                        </div>
                    </td>
                    <td class="px-6 py-4">María López</td>
                    <td class="px-6 py-4">maria.lopez@example.com</td>
                </tr>

                <tr class="border-t border-[color:var(--border)] hover:bg-[color:var(--light-bg)] transition-colors">
                    <td class="px-6 py-4 text-center">
                        <div class="flex items-center justify-center gap-4">
                            <a href="#" class="text-[color:var(--primary)] hover:text-[color:var(--primary-dark)] transition-colors" title="Editar">
                                <i data-lucide="pencil-line" class="w-5 h-5"></i>
                            </a>
                            <a href="#" class="text-[color:var(--error)] hover:text-red-700 transition-colors" title="Eliminar">
                                <i data-lucide="trash-2" class="w-5 h-5"></i>
                            </a>
                            <a href="#" class="text-[color:var(--secondary)] hover:text-emerald-600 transition-colors" title="Ver Detalles">
                                <i data-lucide="eye" class="w-5 h-5"></i>
                            </a>
                        </div>
                    </td>
                    <td class="px-6 py-4">Carlos Vega</td>
                    <td class="px-6 py-4">carlos.vega@example.com</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        lucide.createIcons();
    </script>
}
